#include <iostream>
#include <math.h>
#include <string>
using namespace std;

/* 
Answer:
Since the function calls for a char and inputing some other type of variable ends 
up just removing the input all together and it outputs a empty line or it won't compile.
*/

/*
void print_twice(char phil) {
    cout << phil << phil << endl;
}

int main() {
    char argument = 'b';
    int yipee = 7;
    float me_OOOW = 4.20;
    string amongus = "SUS";
    
    print_twice('a');
    print_twice(argument);
    print_twice(yipee);
    print_twice(me_OOOW);
    print_twice(amongus);

    return 0;
}
*/